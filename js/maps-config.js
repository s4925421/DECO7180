/**
 * Optional map settings (Leaflet + OpenStreetMap — no API key required).
 * Example: use a different tile provider or mirror.
 */
window.PARKQUEST_MAP_CONFIG = window.PARKQUEST_MAP_CONFIG || {
  tileUrl: "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",
};
